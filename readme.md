after cloning remmember to set your settings.
It's safe to set passwords in here because the file is under gitignore

    cp inventory/host_var/localhost.yml.example inventory/host_var/localhost.yml

TODO: